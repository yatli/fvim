<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fvim="clr-namespace:FVim;assembly=FVim"
        mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="300"
        x:Class="FVim.MainWindow"
        CanResize="True"
        Icon="..\Assets\fvim.png"
        Title="fvim"
        Height="{Binding Height, Mode=TwoWay}"
        Width="{Binding Width, Mode=TwoWay}"
        WindowState="{Binding WindowState, Mode=TwoWay}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        >
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="{Binding CustomTitleBarHeight}" />
      <RowDefinition Height="1*" />
    </Grid.RowDefinitions>
    <fvim:TitleBar Grid.Row="0" IsVisible="{Binding UseCustomTitleBar}" HorizontalAlignment="Stretch" />
    <fvim:Editor Grid.Row="1" DataContext="{Binding MainGrid}" />
  </Grid>
</Window>
