<Canvas xmlns      = "https://github.com/avaloniaui"
        xmlns:x    = "http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d    = "http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc   = "http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fvim = "clr-namespace:FVim;assembly=FVim"
        Focusable  = "True"
        x:Class    = "FVim.Editor"
        Background = "{Binding BackgroundBrush, Mode=TwoWay}"
        >
    <Image 
        Name                = "FrameBuffer"
        Source              = "{Binding FrameBuffer,  Mode=TwoWay}"
        Width               = "{Binding BufferWidth,  Mode=TwoWay}"
        Height              = "{Binding BufferHeight, Mode=TwoWay}"
        Stretch             = "Fill"
        HorizontalAlignment = "Left"
        VerticalAlignment   = "Top"
    />
    <fvim:Cursor
        DataContext         = "{Binding CursorInfo}"
        HorizontalAlignment = "Left"
        VerticalAlignment   = "Top"
        Height              = "{Binding h}"
        Width               = "{Binding w}"
        Canvas.Left         = "{Binding x}"
        Canvas.Top          = "{Binding y}"
        RenderTick          = "{Binding RenderTick, Mode=TwoWay}"
    />

</Canvas>
