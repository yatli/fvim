<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fvim="clr-namespace:FVim;assembly=FVim"
        mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="300"
        x:Class="FVim.MainWindow"
        Height="{Binding WindowHeight, Mode=OneTime}"
        Width="{Binding WindowWidth, Mode=OneTime}"
        CanResize="True"
        Title="fvim" >
  <fvim:Editor VerticalAlignment="Stretch" HorizontalAlignment="Stretch" GridId="1" />
</Window>
